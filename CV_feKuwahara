#include <iostream>
#include <math.h>

#include <opencv2/core.hpp>
#include <opencv2/highgui.hpp>
#include <opencv2/imgproc.hpp>

using namespace std;
using namespace cv;

int main()
{
	string str = "MyPath";
	Mat img = imread(str + "Lena.bmp", 0);
	if (img.empty())
		return -1;
	namedWindow("Original", WINDOW_AUTOSIZE);
	imshow("Original", img);
  
  
	/*--Comentario de prueba 1--*/
	/*--- END OF PROGRAM ---*/
	waitKey(0);
	return 0;
}
